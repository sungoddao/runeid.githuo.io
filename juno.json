{
  "satellite": {
    "satelliteId": "dvrig-wqaaa-aaaal-advnq-cai",
    "source": ".",
    "ignore": ["juno.json", ".github/**/*", "CNAME", "LICENSE"]
  }
}
await Promise.all([
  initJuno({
    satelliteId: import.meta.env.VITE_SATELLITE_ID
  }),
  initOrbiter({
    satelliteId: import.meta.env.VITE_SATELLITE_ID,
    orbiterId: import.meta.env.VITE_ORBITER_ID
  })
]);
